# Prompt for Practice Two (Template)

```text
Ты пишешь код для практической работы №2 по Java.
Учитывай глобальный промпт проекта и правила ниже.

Контекст именно этой практики:
- Тема: [Ознакомиться с возможностями языка Java по созданию классов и объектов.]
- Что нужно реализовать: [Главная функция программы содержит массив объектов. Для вашего варианта необходимо предложить и реализовать метод, который будет возвращать значение, основанное на имеющихся полях. Например, для автомобиля, на основе его массы и количества пассажирских мест можно возвращать класс необходимых прав B, C, D. В моем случае нужно написать для Человек. Необходимо реализовать приложение, которое имеет простой текстовый интерфейс и меню, состоящее как минимум из шести пунктов:

Добавление пустого объекта к массиву (используется конструктор по умолчанию).
Добавление объекта с данными, заполненными пользователем (используется конструктор с параметрами).
Редактирование любого поля (по выбору пользователя) любого объекта (по индексу).
Вывод в консоль информации обо всех объектах, включая результат самостоятельно придуманной функции из общей постановки задачи.
Сортировка массива (списка) объектов по любому из полей (по возрастанию).
Завершение работы программы]
- Ограничения: [допускается использование только ArrayList. Обработку ошибок выполнять с помощью условных операторов. Использование механизма исключений — только в крайних случаях. Ввод каждого из полей (атрибутов) класса должен обрабатываться. Минимально — не изменять состояние поля, если в "сеттер" переданы некорректные данные ("защита от дурака").
Результат не может быть выведен без обработки введённых данных.]

Требования к ответу:
1) Дай краткий план решения.
2) Напиши полный рабочий код.
3) Добавь пример входных/выходных данных.
4) Кратко объясни, почему решение соответствует условиям.
5) Если не хватает данных — задай уточнения до генерации кода.
6) Используй английские имена классов, методов и переменных (согласно Google Java Style Guide).
```
